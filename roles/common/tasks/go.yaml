- name: install golang
  ansible.builtin.dnf:
    name: golang
    state: present
- name: run go commands
  ansible.builtin.command: "{{ item }}"
  args:
    chdir: /app
  loop:
    - go mod init dispatch
    - go get
    - go build
 